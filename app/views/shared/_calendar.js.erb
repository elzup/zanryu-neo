$('#calendar').fullCalendar({
  editable: true,
  disableDragging: true,
  events: [
    <% requests.each do |request| %>
    {
      title: '<%= request.room.label %>',
      start: '<%= request.date.strftime('%Y-%m-%d') %>',
      allDay: true
    },
    <% end %>
  ]

});
